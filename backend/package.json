{
  "name": "klip-backend",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts src/token-scope.ts --target=node --format=cjs --outdir=dist",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test tests"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  }
}
